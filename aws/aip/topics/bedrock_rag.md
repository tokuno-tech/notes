# Bedrock RAG / ベクトルストア選定

## RAG構成の全体像

```
ドキュメント（S3 等）
  ↓ チャンク分割・埋め込み生成
ベクトルストア（OpenSearch / Aurora pgvector / Pinecone 等）
  ↓ クエリ → 類似検索（k-NN/ANN）+ メタデータフィルタ
取得した関連チャンクをコンテキストとしてプロンプトに挿入
  ↓
Bedrock FM（Claude など）→ 回答生成
```

Bedrock Knowledge Bases はこの一連を**マネージドで自動化**してくれる仕組み。

---

## インテリジェント検索とは（基本概念）

### 従来の検索 vs インテリジェント検索

| | 従来の検索（キーワード検索） | インテリジェント検索 |
|---|---|---|
| **仕組み** | 入力した単語と一致する文書を探す | 質問の**意図・意味**を理解して答えを探す |
| **質問例** | "有給 申請 方法" | "有給を取るにはどうすればいいですか？" |
| **返すもの** | 関連文書のリスト | **直接的な回答**（該当箇所を抜粋） |
| **同義語** | "有給"と"年次休暇"は別扱い | 同じ意味として認識 |
| **AWSサービス例** | ElasticSearch（旧）, BM25 | **Amazon Kendra** |

```
例：「育児休業は何日取れますか？」という質問に対して

キーワード検索 → "育児休業" "何日" にマッチする文書を列挙（自分で読んで探す）

インテリジェント検索（Kendra）→ 社内規則ドキュメントから
                               「育児休業は最大〇〇日取得可能です」という
                               答えの文章を直接抜き出して提示
```

- **Amazon Kendra** = AWSにおけるインテリジェント検索の代表サービス
- MLベースのセマンティック＋キーワードのハイブリッド検索（純粋なキーワード検索ではない）
- 自然言語の質問に対して**直接回答を抜粋して返す**のが特徴

---

## Kendra vs OpenSearch（RAGでの使い分け）

両方ともBedrockに接続できるが、**設計思想と適用シーンが根本的に異なる**。

### サービスの本質

| | Amazon Kendra | Amazon OpenSearch（Service / Serverless） |
|---|---|---|
| 本来の用途 | **エンタープライズ検索**（社内ドキュメント検索エンジン） |  全文検索 + 分析 + ベクトル検索の汎用エンジン |
| 検索方式 | ML組み込みの**意味検索**（埋め込みは内部で自動生成） | 全文検索（BM25）+ **k-NN/ANN によるベクトル検索** |
| 埋め込みの扱い | **ユーザーが意識しなくてよい**（Kendraが裏で生成） | **ユーザーが埋め込みを生成して投入**する必要あり |
| データソースコネクタ | S3・SharePoint・Confluence・Salesforce 等の**公式コネクタ多数** | 基本はS3 + 自前のETL（Bedrock Knowledge Bases経由なら自動化） |
| インフラ管理 | フルマネージド（インデックス管理のみ） | OpenSearch Service：ノード管理あり / **Serverless：自動スケール** |

### Bedrock との接続方式

| | Kendra | OpenSearch Serverless |
|---|---|---|
| Bedrock Knowledge Bases のベクトルストアとして使えるか | **✕（公式サポートなし）** | **◯（公式サポートあり）** |
| Bedrock からの利用方法 | RetrieveAndGenerate API で Kendra をデータソース指定 | Knowledge Bases 経由で透過的に利用 |
| カスタム埋め込みモデルの選択 | **不可**（Kendra内蔵） | **可**（Titan・Cohere Multilingual 等を選択） |

### 適用シーンの判断軸

| シーン | 推奨 | 理由 |
|---|---|---|
| 社内文書を自然言語で検索したい（埋め込みを管理したくない） | **Kendra** | コネクタが多く、埋め込みを意識せず使える |
| 自前で生成した埋め込みを大量に管理したい | **OpenSearch** | カスタム埋め込みモデルに対応 |
| Bedrock Knowledge Bases で RAG パイプラインを構築したい | **OpenSearch Serverless** | Knowledge Bases の公式ベクトルストア |
| 多言語（日本語・英語・中国語等）を**単一空間**で扱う | **OpenSearch + Cohere Embed Multilingual** | 多言語埋め込みモデルを明示的に選べる |
| メタデータ（カテゴリ・日付・地域）で絞り込み + 類似検索 | **OpenSearch** | k-NN + keyword/date フィルタの同時実行が可能 |
| 数百万件以上のベクトルを低レイテンシで検索 | **OpenSearch（HNSW/IVF）** | ANN専用アルゴリズムで最適化済み |
| インフラ管理を完全に避けたい | **OpenSearch Serverless** | OCU課金で自動スケール、ノード管理不要 |

## OpenSearch Serverless ベクトルコレクション（AIP-19）

### 構成

**Bedrock Knowledge Bases + OpenSearch Serverless（ベクトルコレクション）+ Bedrock FM（Claude）**

```
ドキュメント（S3）
  ↓ Bedrock Knowledge Bases が同期
チャンク分割 → 埋め込み生成（Titan / Cohere Multilingual）
  ↓
OpenSearch Serverless（ベクトル + メタデータ）
  ↓ k-NN（HNSW）+ メタデータフィルタ
取得チャンク → Claude にコンテキスト挿入
  ↓
回答生成
```

### 各要件との対応

| 要件 | 対応する仕組み |
|---|---|
| 約800万件のベクトル埋め込み | OpenSearch Serverless（HNSWでミリ秒検索） |
| 製品カテゴリ・作成年・対象地域でフィルタ | `keyword` / `date` 型メタデータフィールドで k-NN と**同時クエリ** |
| 日本語・英語・中国語の多言語対応 | **Cohere Embed Multilingual**で単一ベクトル空間に統合 |
| インフラ管理の最小化 | Serverless：ノード・シャード・スケーリング自動 |
| 低レイテンシ | HNSW + OCU自動スケール |
| RAGパイプライン構築 | Bedrock Knowledge Bases が同期〜回答生成まで一貫提供 |

### OpenSearch Serverless の料金モデル

- **OCU（OpenSearch Compute Units）単位**で課金
- 検索処理量に応じて自動スケール → 固定インスタンス料金なし
- アクセス変動の大きいチャットボット用途と相性がよい

---

## セマンティックキャッシュ（AIP-17 再掲・参照）

「表現は違うが同じ意図」の問い合わせを意味ベクトルで検出し、Bedrock呼び出しを削減するパターン。詳細は [bedrock_guardrails.md](./bedrock_guardrails.md) のセマンティックキャッシュセクション参照。

### 要点

- **完全一致系（ElastiCache のハッシュキー等）では意味の類似は検出不可**
- **キーフレーズ抽出（Comprehend）は構文レベル**で、意味レベルではない
- **OpenSearch k-NN + 埋め込み**が唯一意味ベースのキャッシュを実現できる組み合わせ

---

## ベクトルストア選定フローチャート

```
Bedrock Knowledge Bases を使いたい？
  ├─ Yes → 公式サポートされるストアから選択
  │         （OpenSearch Serverless / Aurora pgvector / Pinecone / Redis Enterprise 等）
  │         インフラ管理を避けたい → OpenSearch Serverless
  │
  └─ No  → 社内文書検索が主目的？
            ├─ Yes（埋め込みを意識したくない）→ Kendra（RetrieveAndGenerate）
            └─ No（カスタム埋め込みを大規模管理）→ OpenSearch Service / Serverless
```

---

## Knowledge Bases の仕組み：S3 のベクトル化はサービスに含まれるか？

**はい、含まれる。** S3 にドキュメントを置いて同期を実行するだけで、以下が全自動で動く。

```
S3 のドキュメント（PDF / Word / HTML / CSV など）
  ↓ ① 読み込み
  ↓ ② チャンク分割（固定サイズ / セマンティック / 階層的 から選択）
  ↓ ③ 埋め込みモデルで各チャンクをベクトル化
       （Titan Embeddings / Cohere Multilingual 等を選択）
  ↓ ④ ベクトルストアへ格納
       （OpenSearch Serverless / Aurora pgvector / Pinecone 等）
```

**ユーザーがやること**：S3 にドキュメントを置く、Knowledge Bases を設定する、更新時に同期を走らせる

**Knowledge Bases が自動でやること**：埋め込みモデルの呼び出し・チャンキング・ベクトルストアへの書き込み

→ 「Bedrock Knowledge Bases はマネージド RAG パイプライン」という一文が指しているのは、この一連の処理がフルマネージドである点。

---

## Knowledge Bases ソース帰属 + Converse API（AIP-22）

### 要件キーワードと対応機能

| 要件 | 対応機能 |
|---|---|
| 社内ドキュメントから関連情報を検索して回答に使用 | **Knowledge Bases for Amazon Bedrock**（RAG） |
| 回答の根拠となった参照元ドキュメントを明示 | **ソース帰属（Source Attribution）機能** |
| 結論までの判断過程を段階的に提示 | **Converse API** + Claude のチェーン推論 |
| 応答時間 3 秒以内 | Knowledge Bases + Converse API（軽量構成） |
| インフラ管理負担を最小化 | Knowledge Bases + Bedrock（フルマネージド） |

### ソース帰属（Source Attribution）とは

「**回答生成に使ったドキュメントの出典情報を自動付与する機能**」。論文の脚注に相当するものが API レスポンスに自動で入ってくるイメージ。

```
質問：「社員の有給休暇は何日ですか？」

通常の LLM 回答（Knowledge Bases なし）
  → 「有給休暇は 20 日です」
  ← どこに書いてあったか不明。ハルシネーションの可能性あり。

ソース帰属あり（Knowledge Bases）の回答
  → 「有給休暇は 20 日です」
     + 引用元：s3://my-bucket/HR-規定2024.pdf
     + 引用箇所テキスト：「正社員の年次有給休暇は 20 日とする（第 14 条）」
```

Knowledge Bases を RetrieveAndGenerate API または Converse API 経由で使うと、レスポンスに **引用情報（citations）が自動付与**される。

```json
{
  "output": "税率は 10% です。",
  "citations": [
    {
      "retrievedReferences": [
        {
          "content": { "text": "消費税率は 10% です。" },
          "location": { "s3Location": { "uri": "s3://bucket/doc.pdf" } }
        }
      ]
    }
  ]
}
```

- 参照元 S3 URI・ドキュメント名・引用チャンクのテキストがレスポンスに含まれる
- **別途記録するコードを書く必要はない**（引用情報はAPIレスポンスに含まれる）
- 引用できる根拠がある＝ハルシネーションのリスクが下がる（根拠なき生成が難しくなる）

### Converse API とは

複数のモデルを**統一インターフェース**で呼び出せる Bedrock の API。

```
InvokeModel API（旧来）
  Claude → { "anthropic_version": ..., "messages": [...] }
  Titan  → { "inputText": ..., "textGenerationConfig": {...} }
  Llama  → { "prompt": ..., "max_gen_len": ... }
  ← モデルを変えるたびにコードを書き直す必要がある

Converse API
  全モデル共通フォーマット → modelId を変えるだけで切り替え可能
```

**マルチターン対話の仕組み**：会話履歴（messages 配列）をそのまま次のリクエストに渡すことで文脈を維持する。

```python
messages = [
    {"role": "user",      "content": [{"text": "AWS とは？"}]},
    {"role": "assistant", "content": [{"text": "AWS は..."}]},
    {"role": "user",      "content": [{"text": "その中で S3 とは？"}]},
    # ← 「その中で」が何を指すかを理解できる
]
```

### RetrieveAndGenerate API とは

Knowledge Bases を使った RAG を**1回の API 呼び出しで完結**させる API。

```
自前実装の RAG（3ステップ必要）
  ① Retrieve API でベクトル検索 → 関連チャンクを取得
  ② 取得チャンクをプロンプトに組み込む（自前コード）
  ③ InvokeModel / Converse API でモデルを呼ぶ

RetrieveAndGenerate API（1ステップ）
  質問を渡すだけで ①②③ を内部でまとめて実行
  → 回答 + citations（ソース帰属）が自動で返ってくる
```

**Retrieve API との違い**

| API | 何をするか | 使いどころ |
|---|---|---|
| **Retrieve API** | ベクトル検索のみ（モデル呼び出しなし） | 検索結果を自前でハンドリングしたい場合 |
| **RetrieveAndGenerate API** | 検索 + 生成 + citations 付与を1回で完結 | 標準的な RAG ユースケース |

**マルチターン対話**：`sessionId` を引き継ぐことで会話の文脈を維持できる。

### Converse API vs RetrieveAndGenerate API（使い分け）

| | Converse API | RetrieveAndGenerate API |
|---|---|---|
| 主な用途 | 汎用モデル対話（RAG なしも含む） | Knowledge Bases を使った RAG |
| RAG の組み込み | 自前で Retrieve してプロンプトに挿入 | **自動**（1回の呼び出しで完結） |
| ソース帰属（citations） | 自前実装が必要 | **自動付与** |
| モデル切り替え | modelId を変えるだけ | KB 設定で指定 |
| マルチターン管理 | messages 配列 | sessionId |

- Knowledge Bases と組み合わせることで、RAG + マルチターン + ソース帰属を一体で実現

## マルチテナント Knowledge Base 構成（AIP-27）

### 要件

- 複数テナント（ビルごと）のデータを**厳密に分離**
- 空室状況の問い合わせに**ほぼリアルタイムの応答**
- 管理スタッフのアクセスを**テナント単位で制御**
- 繁忙期でも安定した応答速度

### 構成

**中央集権的な Bedrock Agent（ビルごとの KB をオーケストレーション）+ IAM Identity Center**

```
管理スタッフ
  ↓ IAM Identity Center（許可セット）でアクセス制御
     → 自分のビルの KB のみ参照可能
  ↓
Bedrock Agent（中央集権的に複数 KB を管理）
  ├─ ビル A の Knowledge Base（静的な運用ルール・テナント情報）
  ├─ ビル B の Knowledge Base
  └─ アクショングループ → BMS API を直接呼び出す（リアルタイム空室情報）
```

### ポイント①：リアルタイム性はエージェントのアクショングループで実現

```
【間違いパターン】
BMS → SQS → Lambda → KB を更新 → 検索
         ↑ KB のインジェスト処理に時間がかかる → リアルタイム不可

【正解パターン】
BMS → エージェントのアクショングループが API を直接呼び出す
         ↑ 最新データをその場で取得 → リアルタイム対応
```

**「頻繁に変わるデータ（空室状況）」は KB に同期するのではなく、Agent が直接 API 呼び出し。**
KB はあくまで「静的に近いドキュメント（運用ルール・テナント契約等）」に使う。

### ⚠️ OpenSearch のメタデータフィルタ ≠ 厳密なデータ分離

OpenSearch では k-NN クエリに `filter`（メタデータフィルタ）を付与することでテナントごとにデータを絞り込める。
ただし、これは**論理的なフィルタリング**であり**物理的・厳密な分離ではない**。

```
論理的フィルタリング（OpenSearch メタデータフィルタ）
  → 同一インデックス内でクエリ時に条件を絞るだけ
  → フィルタの設定ミスやバグがあると他テナントのデータが漏洩するリスクがある
  → ストレージレベルでは全テナントのデータが混在している

厳密な分離
  → テナントごとに別インデックス or 別 KB
  → 物理的にデータが混在しないため漏洩リスクがない
```

「厳密な分離（strict isolation）」が要件のときは、単一インデックス + メタデータフィルタではなく、テナントごとに KB（＝別インデックス）を作成する。

---

### ポイント②：KB は 1 アカウントあたり約 10 個の上限あり

ビルごとに KB を作成する方針自体は有効だが、ビル数が増えるとクォータに達する。
大規模展開ではメタデータフィルタリングとの組み合わせや、クォータ引き上げリクエストが必要。

### ポイント③：アクセス制御は IAM Identity Center（Cognito ではない）

| | Amazon Cognito | AWS IAM Identity Center |
|---|---|---|
| 主な用途 | **Web/モバイルアプリのユーザー認証** | **組織の AWS リソースへのアクセス管理（SSO）** |
| 向いているケース | サインアップ・ログイン・ソーシャルログイン | ビル管理スタッフが特定の KB だけにアクセスできるよう制御 |
| AWS リソース制御 | Identity Pool 経由で間接的に | **許可セット（Permission Set）で直接 IAM ロールを割り当て** |

→「AWS リソース（Bedrock KB）へのアクセスをテナント単位で制御」→ **IAM Identity Center が正解**

## ハルシネーション削減アプローチ（AIP-30）

### 問題：ハルシネーション（原文にない情報を生成）

現象：文書全体をそのままプロンプトに入力するだけでは、FM が原文にない統計データ・研究結果を生成する。

### 正解：2 つの組み合わせ（複数選択）

#### ① ゼロショット Chain-of-Thought（CoT）プロンプティング

```
プロンプトの設計：
「要約を生成する前に、以下のステップを実行すること。
 ①ソース文書のどの部分に根拠があるかを特定する
 ②その根拠と結論の論理的なつながりを説明する
 ③根拠のない情報は含めないこと」
```

- **追加インフラ不要**（プロンプト変更のみ）
- モデルが明示的に「ソース文書との照合」を行うため、ハルシネーションが減少
- 「ゼロショット」= Few-shot の例示なしで、指示だけで推論プロセスを誘導

#### ② RAG（Bedrock Knowledge Bases + セマンティックチャンキング）

```
ドキュメント
  ↓ セマンティックチャンキング（意味のまとまりで分割）
  ↓ 埋め込みモデルでベクトル化
ベクトルストア
  ↓ クエリに最も関連するチャンクを取得
FM（「提供されたコンテキストのみを使って要約すること」と指示）
  ↓
コンテキストに基づいた回答
```

- FM に「取得したコンテキストに根拠を置く」という制約が生まれ、ハルシネーションが減少
- セマンティックチャンキングにより文脈の切れ目が少なく、検索精度が向上

### temperature とハルシネーションの関係（試験頻出）

```
temperature = 0    → 最も確実な（高確率の）トークンを選択 → ハルシネーション少ない
temperature = 1.0  → 確率分布の多様性を最大化 → ハルシネーション増加
```

---

### ⚠️ CoT（Chain-of-Thought）≠ Extended Thinking（拡張思考）

混同しやすいが別物。

```
Chain-of-Thought（CoT）プロンプティング
  = プロンプトの設計技法
  = 「ステップバイステップで考えてください」という指示をプロンプトに加える
  = モデルの推論プロセスを出力に含めさせて、照合・確認ステップを強制
  = 追加インフラ不要・プロンプト変更のみで即導入可能
  = ハルシネーション防止に有効

Extended Thinking（拡張思考）
  = Anthropic Claude の特定機能
  = モデルが内部で長い推論トークンを消費してから回答を生成する
  = 主に数学・コード等の複雑推論タスク向け
  = トークン増加により大幅なレイテンシー悪化（3秒以内の要件を満たしにくい）
  = RAG の代替にはならない
```

---

### ⚠️ 「要約なのになぜ RAG？」の疑問への回答

「文書を要約するならその文書を直接渡せばいい」という直感は正しい。ただし、長大な臨床試験レポートを丸ごとプロンプトに入力することには問題がある。

```
文書全体をそのまま入力する問題
  → コンテキストウィンドウの圧迫（モデルが全体を均等に注意できない）
  → 入力が長すぎると FM が情報を正確にトレースしにくくなる
  → ハルシネーションが発生しやすい

RAG（セマンティックチャンキング）を使う意味
  → 文書を意味のまとまりで分割し、必要な部分だけを取得してコンテキストに渡す
  → FM に「提供されたチャンクのみを使って要約すること」と制約をかけられる
  → モデルが「取得されたコンテキストに根拠を置く」形になりハルシネーション減少
  → 入力量が減り、レイテンシーも改善しやすい
```

**整理**：RAG は「知識ベースから情報を引っ張ってくる」だけでなく、「対象文書を分割して根拠付き生成を強制する」グラウンディング手法としても使う。

---

## チャンキング戦略（Bedrock Knowledge Bases）

文書をベクトルストアに格納する際に、どう分割するかを決める設定。分割単位が検索精度・文脈保持・レイテンシーに直接影響する。

### 種類と比較

| 戦略 | 分割の基準 | 向いているケース |
|---|---|---|
| **固定サイズチャンキング** | 文字数・トークン数で機械的に分割 | 構造が均質なドキュメント / シンプルな設定で良い場合 |
| **セマンティックチャンキング** | 意味のまとまり（文章の区切れ目）で分割 | 臨床試験レポート・法令文書など構造化された長文 |
| **階層的チャンキング** | 親チャンク（大きい単位）と子チャンク（小さい単位）の二層構造で分割 | 概要レベルの検索と詳細レベルの検索を両立したい場合 |

---

### 固定サイズチャンキング

```
文書を N トークン（or N 文字）ごとに機械的に切る
オーバーラップ（重複）を設定することで文脈の切れ目を緩和できる

例：512 トークンで分割、オーバーラップ 50 トークン
  [...文章A | 文章B | 文章C...]
         [重複]   [重複]
```

- シンプルで実装コストが低い
- 文章の途中で切れて文脈が失われるリスクがある
- 「意味の切れ目」を考慮しないため、同じ話題が複数チャンクに分断されやすい

---

### セマンティックチャンキング

```
文の埋め込みベクトルを比較し、意味的に大きく変化する箇所で分割する
→ 段落・節の区切りに近い単位でチャンクが作られる

例：臨床試験レポート
  [患者背景セクション] [投与量・手順セクション] [結果・統計セクション] ...
  → 各セクションが意味的なまとまりとして 1 チャンクになる
```

- 固定サイズより検索精度が高い（関連する情報がチャンク内に収まりやすい）
- 構造化された長文ドキュメントに特に有効

---

### 階層的チャンキング（Hierarchical Chunking）

```
文書を 2 つの粒度で保持する

親チャンク（大きい）：章・節レベルのまとまり → 文脈の把握に使う
子チャンク（小さい）：文・段落レベルの細粒度 → 精密な検索に使う

検索フロー
  クエリ → 子チャンクで類似検索 → ヒットした子チャンクの「親」を返す
```

- 検索は細かい粒度で行い、回答生成は広い文脈で行うという二段構え
- 「どの段落が関連するか」を見つけつつ「その段落が属するセクション全体」を FM に渡せる
- 長文で細部の検索と広い文脈の保持を両立したいケースに向く

---

### コンテンツタイプ別前処理パイプライン（Task 5.2）

コンテンツの種類によって最適な前処理戦略が異なる。「全種類を固定サイズで同一処理」するのではなく、専用パイプラインを用意することで検索精度・文脈品質が向上する。

| コンテンツタイプ | 前処理のポイント |
|---------------|---------------|
| **技術ドキュメント** | 見出し・章構造を活かした階層型チャンキング |
| **会話テキスト** | 発言（ターン）単位で分割・話者情報を保持 |
| **コード** | 関数・クラス単位で分割（構文を壊さない）。コメント除去でトークン節約も可 |

### コンテンツ処理の自動テストフレームワーク（Task 5.2）

チャンキング・前処理パイプラインの品質を自動で検証する仕組み。特定ドキュメントで動いてもサイズ・タイプが変わると壊れるケースを防ぐ回帰テスト的な位置づけ。

```
① テストデータセット（小・中・大サイズ × 各コンテンツタイプ）
② 期待値（チャンク数・オーバーラップ率・トークン数の範囲）
③ 自動実行（CI/CD に組み込み）→ CodePipeline で品質ゲート化
④ 結果を CloudWatch へ送信してモニタリング
```

→ 試験で「コンテンツ処理の品質を継続的に担保」文脈 → 自動テスト ＋ CodePipeline が正解候補

### 前処理正規化チェック（Task 5.2）

埋め込みモデルに渡す前にドキュメントの形式・品質を統一する検証。非一貫性があると同一概念の埋め込みがバラつき、検索精度が落ちる。

```
① 文字コード統一（UTF-8）
② 空白・改行の正規化（全角スペース → 半角 等）
③ 特殊文字・制御文字の除去
④ 最小チャンク長フィルタ（短すぎるチャンクを除外）
⑤ 言語の一致確認（埋め込みモデルと同じ言語か）
```

### 埋め込み可視化診断（Task 5.2）

高次元の埋め込みベクトルを2次元に次元削減して可視化し、ベクトル空間の問題を診断する手法。

```
高次元ベクトル（例：1536次元）→ UMAP / t-SNE で次元削減 → 2D プロット
```

| 可視化で見える問題 | 原因 |
|-----------------|------|
| 同カテゴリが散らばる | 埋め込みモデルがドメインに不適合 |
| 孤立した点（外れ値） | 前処理の失敗・異常なチャンク |
| 全体が1箇所に密集 | モデルがドメインを区別できていない |

→ SageMaker Processing Job や OpenSearch の k-NN デバッグで確認

---

## Bedrock は埋め込み（ベクトル化）もできる

### Bedrock の埋め込みモデル

```
Amazon Bedrock = 生成AIだけでなく、埋め込み（Embedding）生成もできる

主な埋め込みモデル
  Amazon Titan Embeddings V2        ← AWS純正
  Cohere Embed（英語 / 多言語）      ← 多言語対応

呼び出し方：InvokeModel API（生成と同じ）
  → テキスト → float配列（ベクトル）が返ってくる
  → モデルのホスティング・GPU・管理は一切不要
```

### HuggingFace とは（混同注意）

```
HuggingFace（ハギングフェイス）
  = AIモデルの GitHub 的なプラットフォーム＋Pythonライブラリ群

  ① Hub       → 数十万のAIモデルが公開（BERT / LLaMA / Stable Diffusion etc.）
  ② transformers ライブラリ → Hubのモデルを Python で使うためのライブラリ
  ③ sentence-transformers  → テキスト→ベクトル変換に特化したライブラリ
```

```python
# 使い方のイメージ
from sentence_transformers import SentenceTransformer
model = SentenceTransformer('all-MiniLM-L6-v2')  # Hub からモデルをDL
vector = model.encode("こんにちは")               # → float配列（ベクトル）
```

ローカル・研究環境では便利だが、**モデルを自分の環境で動かす**設計。

### HuggingFace を Glue で動かす場合の制約

| 問題点 | 内容 |
|---|---|
| GPU サポート | Glue（Apache Spark基盤）はGPUサポートに制約あり → 推論が遅い |
| モデル管理 | 数百MB〜数GBのモデルファイルのキャッシュ・ライブラリ依存関係を自前管理 |
| 運用負荷 | 「運用負荷最小」要件に反する |
| コスト | Bedrock API より高くなるケースがある |

| | HuggingFace（自前実行） | Bedrock 埋め込み API |
|---|---|---|
| 実態 | モデルを自分の環境で実行 | API を叩くだけ |
| 管理 | モデル・GPU・ライブラリを自前管理 | AWS 任せ |
| 向いている場面 | ローカル開発・研究・カスタム学習 | 本番・サーバーレス構成 |

→ 埋め込みが必要なら **Bedrock の InvokeModel を呼ぶだけ**が最もシンプル。

### RAG取り込みパイプライン（サーバーレス構成例）

```
S3（約40GB の JSON）
  ↓
Step Functions Distributed Map（最大10,000並列でS3ファイルを処理）
  ↓ 各ファイルに対して
Comprehend DetectPiiEntities API → PIIをマスク（コード不要）
  ↓
Bedrock InvokeModel（Titan Embeddings V2）→ ベクトル化
  ↓
OpenSearch Serverless（ベクトル格納・k-NN検索）
```

全工程がマネージドAPIの呼び出しのみ → 自前コードほぼゼロ。

### サーバーレス要件でのストレージ選定

| サービス | サーバーレス? | 備考 |
|---|---|---|
| **OpenSearch Serverless** | ◯ | OCU自動スケール、クラスター管理不要 |
| OpenSearch Service（マネージドクラスター） | ✗ | ノード数・インスタンスタイプの選定・管理が必要 |
| RDS for PostgreSQL（pgvector） | ✗ | インスタンス管理・パッチ適用が必要 |
| DocumentDB | ✗ | ベクトル検索が限定的、クラスター管理あり |

---

## OpenSearch 重要用語

### BM25（Best Match 25）

OpenSearch / Elasticsearch のデフォルト**全文検索スコアリングアルゴリズム**。

- **TF（Term Frequency）**：文書内での単語出現頻度
- **IDF（Inverse Document Frequency）**：全文書中での単語の希少さ
- 2つを組み合わせて「この文書にとってこの単語がどれだけ重要か」をスコア化

| 検索方式 | アルゴリズム | 得意なこと |
|---|---|---|
| 全文検索 | **BM25** | キーワードが含まれる文書を優先 |
| セマンティック検索 | **k-NN（HNSW/IVF）** | 意味的に近いベクトルを探す |

> 例：「子供向けビーチホテル」→ BM25はこの単語を含む文書を優先、k-NNは「ファミリー向け海辺リゾート」も意味的に近いとして引っかける

### JVM ヒープサイズ

OpenSearch は JVM（Java VM）上で動作。ヒープ = JVM が使えるメモリ領域。

| 設定 | 影響 |
|---|---|
| 小さすぎる | GC が頻繁に発生 → レイテンシースパイク |
| 大きすぎる | GC 1回の停止時間が長くなる（Stop-the-World） |
| **推奨** | 物理メモリの50%以下、かつ上限31GB（Compressed OOPs が効く境界） |

### Auto-Tune

OpenSearch の**自動パフォーマンス最適化機能**。

- JVM ヒープサイズ、スレッド数、キューサイズ等を自動調整
- `_cluster/settings` で有効化
- 手動チューニング不要でレイテンシーを安定化

### Step Functions Distributed Map とは

```
S3上のオブジェクトを自動列挙 → 最大10,000並列で各ファイルを処理

通常の Map ステート     → 配列データのイテレート
Distributed Map ステート → S3インベントリ・S3リストから直接取得して大規模並列
```

- リトライ・エラーハンドリングは Step Functions の組み込み機能で対応
- 個別のエラー処理コードを書く必要なし

---

## GraphRAG（AIP-70）

### 通常RAGとの根本的な違い

**通常RAG**：ドキュメントをチャンクに分割 → ベクトル埋め込み → クエリと類似するチャンクを返す。  
各チャンクは**独立していて、チャンク間の関係性は無視**される。

**GraphRAG**：ドキュメントから**エンティティ（人・物・概念）と関係（エッジ）を抽出してグラフ構造を構築**し、クエリに対してグラフ上を**多段階でたどる（マルチホップ）**ことで間接的な連鎖関係まで取得できる。

```
通常RAG の限界例（バイオメディカル）：
  質問：「化合物Aが病気Bに効く仕組みは？」
  → 「Aに関するチャンク」「Bに関するチャンク」を別々に取得
  → A→B の間にある連鎖（中間ノード）が見えない

GraphRAG（マルチホップ推論）：
  化合物A → タンパク質X に結合
      ↓
  タンパク質X → 酵素Y を活性化
      ↓
  酵素Y → 病気Bの炎症経路を抑制
  
  → A→X→Y→B という3ホップの推論パスをまとめてコンテキストとして提供
```

### GraphRAG のアーキテクチャ

```
ドキュメント（論文・テキスト）
  ↓ エンティティ抽出・関係抽出（LLM or NLP）
知識グラフ（ノード＋エッジ）
  ↓ ベクトル埋め込みも付与
グラフ＋ベクトルのハイブリッドストア（Neptune Analytics）
  ↓ クエリ → グラフ走査 + ベクトル類似検索
関連ノード・関係パスをコンテキストとして取得
  ↓
Bedrock FM（Claude など）→ マルチホップ推論を踏まえた回答生成
```

### 使うべき場面

| 要件 | 向き |
|---|---|
| エンティティ間の**多段階関係**（化合物→タンパク質→病気） | ✅ GraphRAG |
| 「なぜ」「どのように連鎖しているか」が問われる | ✅ GraphRAG |
| バイオメディカル・創薬・知識グラフQA・不正検知ネットワーク | ✅ GraphRAG |
| 単純な「この件についての記述を教えて」 | 通常RAGで十分 |
| 非構造化テキストの意味的類似検索 | 通常RAGで十分 |

### Bedrock Knowledge Bases の GraphRAG 機能

- Bedrock Knowledge Bases に**GraphRAGモード**が搭載されている
- バックエンドとして **Amazon Neptune Analytics** を使用
- マネージドで知識グラフ構築〜マルチホップ検索〜FM呼び出しを一貫して管理
- 設定方法：Knowledge Bases 作成時に「Graph store」としてNeptune Analyticsを指定

## Amazon Neptune Analytics（AIP-70）

Neptune Database（本体のグラフDB）とは**別サービス**。混同しないこと。

### Neptune Database vs Neptune Analytics

| | Neptune Database | Neptune Analytics |
|---|---|---|
| **種別** | グラフDB（トランザクション用） | グラフ分析エンジン |
| **主な用途** | 知識グラフ・関係データの保存・CRUD | グラフアルゴリズム・分析・GraphRAG |
| **ベクトル検索** | ❌ 非対応 | ✅ 対応（ANN） |
| **GraphRAG** | ❌ | ✅ Bedrock Knowledge Bases と統合 |
| **動作方式** | 永続ストレージ・ACID保証 | グラフをメモリにロードして高速分析 |
| **スケール** | オンライントランザクション向け | バッチ分析・探索クエリ向け |

### Neptune Analytics の仕組み

```
グラフデータ（S3 や Neptune DB から取り込み）
  ↓ メモリ上にグラフをロード
Neptune Analytics
  ├─ グラフアルゴリズム（PageRank、最短経路、コミュニティ検出）
  ├─ ベクトル類似検索（ANN）
  └─ グラフ走査 + ベクトル検索の組み合わせ（GraphRAG用途）
```

### ベクトルストア比較における位置づけ

```
グラフ構造 + ベクトル検索を組み合わせたい → Neptune Analytics（一択）
通常のベクトル類似検索のみ → OpenSearch / MemoryDB / Aurora pgvector を要件で選ぶ
```

※ 本ファイル内のベクトルストア比較（MemoryDB Flat vs HNSW の節）にも Neptune Analytics は記載済み。
- 大量ファイルのバッチ処理オーケストレーションとして最適

---

## Bedrock ナレッジベース メタデータフィルタリング（AIP-81）

### RAGの検索範囲を絞る方法

```
問題：大量ドキュメントがKBにあり、無関係な文書までベクトル検索の対象になる
解決：クエリ時にメタデータでフィルタして、検索母数を絞る
```

### S3 + Bedrock KB のメタデータ付与方法

```
S3 オブジェクト：article_001.txt
メタデータファイル：article_001.txt.metadata.json  ← 同じパスに配置するだけ

メタデータファイルの中身：
{
  "metadataAttributes": {
    "category": "politics",
    "year": "2023",
    "doc_type": "interview"
  }
}
```

### クエリ時のフィルタ指定

```python
# RetrieveAndGenerate API の呼び出し例
response = bedrock_agent.retrieve_and_generate(
    input={"text": "2023年の政治動向について"},
    retrieveAndGenerateConfiguration={
        "knowledgeBaseConfiguration": {
            "retrievalConfiguration": {
                "vectorSearchConfiguration": {
                    "filter": {
                        "andAll": [
                            {"equals": {"key": "category", "value": "politics"}},
                            {"equals": {"key": "year",     "value": "2023"}}
                        ]
                    }
                }
            }
        }
    }
)
```

→ **カテゴリ=政治 AND 年=2023 の文書だけが**ベクトル検索の対象になる

## OpenSearch のブールフィルタ（k-NN + filter）

### ブールフィルタとは

ベクトル類似検索（k-NN）に**条件絞り込み（ブール論理）**を組み合わせる機能。

```
通常のk-NN検索：
  「このクエリに似た上位k件を全ドキュメントから返す」

k-NN + ブールフィルタ：
  「カテゴリ=政治 かつ 年=2023 の文書の中から、このクエリに似た上位k件を返す」
```

```json
// OpenSearch k-NN + ブールフィルタのクエリ例
{
  "knn": {
    "embedding": {
      "vector": [0.1, 0.2, ...],
      "k": 5,
      "filter": {
        "bool": {
          "must": [
            {"term": {"category": "politics"}},
            {"range": {"year": {"gte": 2020}}}
          ]
        }
      }
    }
  }
}
```

→ Bedrock KB の標準機能（メタデータフィルタリング）が**同じことをマネージドで提供**している。

---

## Amazon Kendra のファセット検索とカスタムランキング

### ファセット検索（Faceted Search）とは

**絞り込み条件を動的に提示する検索UI機能。**

```
例：Eコマースの検索結果ページ
  「ブランド: Nike / Adidas / Puma」← これがファセット
  「価格帯: ¥1000-5000 / ¥5000-10000」
  「サイズ: S / M / L」

ユーザーが選ぶとその条件で絞り込まれる
→ 大量の検索結果を段階的に絞り込めるUI
```

Kendra のファセット検索では、インデックスのフィールドをファセットとして設定しておくと、検索結果に「どのカテゴリが何件ヒットしたか」を返してくれる。

### カスタムランキング（Custom Ranking）とは

検索結果の並び順（ランキング）を**ビジネスルールで調整**する機能。

```
デフォルト：Kendra が判断した関連性スコア順

カスタムランキング：
  ・特定のフィールド値が高いものを上位に表示
  ・新しい日付のものを優先
  ・特定のタグが付いたものを上位に
```

## RAG 検索精度改善パターン（公式模擬 Q16）

### 問題の構造：取得結果の順序が悪い

```
ユーザー報告: 「最も関連性の高い情報が結果の下位に表示される」
→ 検索は十分なドキュメントを取得できているが、ランキングが悪い
→ 解決策: リランキング + ハイブリッド検索
```

### 2つの改善アプローチ

#### 1. Bedrock リランカーモデル（取得後の並べ替え）
- 取得済みのドキュメント群を、クエリとの**セマンティック関連性スコア**で並べ替え
- Bedrock の組み込み機能 → 最小オーバーヘッド
- OpenSearch Service の並べ替えステップと組み合わせて使用

```
クエリ → ベクトル検索（多めに取得）→ Bedrockリランカーで並べ替え → 上位N件をLLMへ
```

#### 2. ハイブリッド検索（KB + OpenSearch Serverless）
- **ベクトル検索**（セマンティック類似度）+ **キーワードマッチング** を組み合わせ
- どちらか片方では拾えないドキュメントをカバーできる
- Bedrock KB の設定で有効化可能 → 最小オーバーヘッド

## ベクトルストア選択（公式模擬 Q18）

### Amazon S3 Vectors（S3 ベクトルバケット）

- S3 のフルマネージド・サーバーレスなベクトル検索機能
- **数十億ベクトル**まで対応
- **使用量課金**（ストレージ + クエリ）→ 低頻度ワークロードに最適
- インフラ管理不要

### ベクトルストア比較（コスト観点）

| サービス | 課金モデル | 低頻度ワークロードのコスト | インフラ管理 |
|---|---|---|---|
| **S3 Vectors** | 使用量課金 | ✅ 低い | 不要 |
| OpenSearch Serverless | **コンピューティングユニット課金** | ❌ 常時課金で割高 | 不要 |
| OpenSearch Service | プロビジョニング課金 | ❌ 割高 | 必要 |
| RDS + pgvector | インスタンス課金 | ❌ 割高 | 必要 |
| DynamoDB + Lambda | カスタム実装 | ❌ ベクトル検索非対応・レイテンシー大 | 必要 |

## ベクトル検索アーキテクチャの最適化

### KNN vs ANN（重要・試験頻出）

| | KNN（厳密最近傍） | ANN（近似最近傍） |
|---|---|---|
| 検索方法 | 全ベクトルを一つ一つ比較 | 賢いアルゴリズムで「近そうな領域」だけ検索 |
| 精度 | 完全に正確 | 99%程度（わずかに落ちる） |
| 速度 | 遅い（大規模で顕著） | 圧倒的に速い |
| 用途 | 完全精度が必要な場面 | レコメンデーション・大規模検索 |

**代表アルゴリズム：HNSW（Hierarchical Navigable Small World）**

---

### OpenSearchのパフォーマンス最適化パターン

同時リクエストでレイテンシーが増加する場合：

| アプローチ | 効果 | 理由 |
|---|---|---|
| **マルチインデックス**（カテゴリ別） | ✅ 有効 | 検索対象ベクトル数を削減 |
| **ANN（近似最近傍）** | ✅ 有効 | 検索自体を高速化 |
| シャーディング | △ 補助的 | 分散はするが検索自体は軽くならない |
| 次元数を増やす | ❌ 逆効果 | 計算量が増えて遅くなる |
| 単一インデックス | ❌ 逆効果 | 大規模化でパフォーマンス低下 |

---

### ベクトルストアアーキテクチャの概念整理

| 概念 | 何で分けるか | 目的 |
|---|---|---|
| **トピックベースのセグメンテーション** | 内容のテーマ | 「インストール」「FAQ」等で分類 |
| **階層的インデックス（Coarse-to-fine）** | 粗さのレベル | 大カテゴリ→小カテゴリの2段階検索 |
| **シャーディング** | データ量 | 複数ノードに分散して並列処理 |
| **マルチインデックス** | 分野・カテゴリ | 検索対象を絞って高速化 |

---

### ベクトルストア選択（シナリオ別）

| シナリオ | 正解 |
|---|---|
| 「RAG・FM統合・頻繁な更新・すぐ使いたい」 | **Knowledge Bases for Bedrock** |
| 「超大規模・トピックセグメンテーション・カスタム検索」 | **OpenSearch + Neural Plugin** |
| 「従来DBとベクトル検索を組み合わせ」 | **RDS + pgvector** |
| 「低頻度・大量ベクトル・コスト最適」 | **S3 Vectors** |
| FaissやPinecone | サードパーティ。試験の正解にはなりにくい（Bedrock Way） |

---

### メタデータフレームワーク

RAGの検索精度を上げるためにドキュメントにメタデータを付与する：
- **timestamp**（S3オブジェクトメタデータ）→ 最新文書のみ検索
- **author / オーサーシップ**（カスタム属性）→ 作成者でフィルタ
- **タグ・カテゴリ**（タギングシステム）→ 分野で絞り込み

「メタデータフィルタリング」= ベクトル検索 + 条件絞り込みの組み合わせ。

---

## ベクトル量子化 vs 検索アルゴリズム（2つの独立した軸）

### 量子化（ベクトルの数値精度）

ベクトル1本のデータをどの精度で持つか：

| 精度 | サイズ | メモリ | 精度 |
|---|---|---|---|
| **FP32**（デフォルト） | 32bit | 100% | 最高 |
| **FP16** | 16bit | 50% | ほぼ同等 |
| **Binary** | 1bit | 3% | 落ちるが超高速 |

### 検索アルゴリズム（n本のベクトルからどう絞り込むか）

| アルゴリズム | 日本語名 | 計算量 | 精度 | 特徴 |
|---|---|---|---|---|
| **FLAT**（brute force） | 全探索 | O(n) | 100%（厳密KNN） | 小規模のみ |
| **HNSW** | 階層的ナビゲート可能スモールワールド | O(log n) | 近似（高精度） | **精度と速度のバランス◎** |
| **IVF** | 転置ファイルインデックス | クラスタ分割で削減 | 近似（ANN） | 大規模・メモリ節約 |
| **LSH** | 局所性鋭敏型ハッシング | ハッシュバケットで絞り込み | 近似（精度低め） | 超大規模・速度優先 |

#### LSH の仕組み
近いベクトルが同じハッシュバケットに入る性質を利用して全件比較を回避する。シンプルで超大規模データに強いが、ハッシュ衝突の影響で HNSW より精度が落ちやすい。

### 量子化（PQ含む）

ベクトル1本のデータをどの精度・形式で持つか：

| 精度/方式 | 日本語名 | サイズ | メモリ | 特徴 |
|---|---|---|---|---|
| **FP32**（デフォルト） | 単精度浮動小数点 | 32bit | 100% | 最高精度 |
| **FP16** | 半精度浮動小数点 | 16bit | 50% | ほぼ同等 |
| **Binary** | 二値量子化 | 1bit | 3% | 精度落ちるが超高速 |
| **PQ** | 直積量子化 | 分割圧縮 | 10〜40倍削減 | メモリ大幅節約・誤差あり |

#### PQ の仕組み
ベクトルを複数のサブベクトルに分割し、各部分を「代表コード」に置き換えて圧縮する。大規模データをメモリに収めるコスト削減手法。IVF と組み合わせた **IVFPQ** が実務でよく使われる。

### 両者は直交（組み合わせ可能）

```
              FLAT          HNSW          IVF / LSH
FP32      厳密・遅い        速い              速い
FP16      厳密・中速     高速・省メモリ        高速
PQ        —           超高速・粗い      超高速・省メモリ（IVFPQ）
```

→ **HNSW + FP16** = アルゴリズムで枝刈り＋数値精度で省メモリ（OpenSearch Serverless推奨構成）
→ **IVF + PQ（IVFPQ）** = 大規模データのメモリ削減が最優先の場合

## Knowledge Bases のバックエンド選択肢

OpenSearch Serverlessがデフォルトだが以下も選択可能：

| バックエンド | 特徴 |
|---|---|
| **OpenSearch Serverless**（デフォルト） | 自動スケール・運用負荷最小 |
| Aurora PostgreSQL（pgvector） | 既存RDS資産活用・SQL連携 |
| Pinecone | サードパーティ特化型ベクトルDB |
| Redis Enterprise Cloud | 低レイテンシ・インメモリ |
| MongoDB Atlas | ドキュメントDB＋ベクトル検索 |
| **Neptune Analytics** | GraphRAG用（グラフ＋ベクトル） |

### 既存OpenSearch + 新規Bedrock の判断フロー

```
既存OpenSearchがある
  ├─ Serverlessか？
  │   ├─ Yes → スキーマ合わせてKB接続可（Bedrock Way）
  │   └─ No（プロビジョンド版）→ KB使えない → 自前RAG
  └─ 検索ロジックが複雑（独自スコアリング等）
      → KB使える場合でも自前RAGを選ぶ価値あり
```

**⚠️ OpenSearch Service（プロビジョンド版）はKB非対応**

---

## pgvector（Aurora PostgreSQL）の実態

### データ構造

```sql
CREATE TABLE documents (
    id        SERIAL PRIMARY KEY,
    content   TEXT,           -- チャンクのテキスト（FMに読ませる単位）
    embedding VECTOR(1536),   -- contentをベクトル化した値
    category  TEXT,           -- メタデータ（SQLフィルタで使う）
    year      INT,
    author    TEXT
);
```

- **content** = チャンク（1文書 → 複数行に分割されて入る）
- **embedding** = そのチャンクのベクトル（メタデータはベクトル化しない）
- メタデータはSQLのWHEREで絞り込む（ベクトル化不要）

### 検索クエリ

```sql
SELECT content FROM documents
WHERE category = '法務'                    -- メタデータフィルタ（SQL）
ORDER BY embedding <=> [0.1, ...]::vector  -- ベクトル距離ソート
LIMIT 5;
```

## OpenSearch ニューラル検索（Neural Search Plugin）

**OpenSearch自身が埋め込みモデルの呼び出しまで自動でやってくれる機能。**

### 通常のベクトル検索との違い

```
通常（自前実装）：
  クエリテキスト → アプリがBedrockを呼んでベクトル化 → OpenSearchにベクトル投げる

ニューラル検索：
  クエリテキスト → OpenSearchが自動でベクトル化 → k-NN検索
  （アプリ側はテキストを投げるだけ）
```

### 仕組み

1. OpenSearchにMLモデルを登録（Bedrock/SageMaker/Hugging Face）
2. Ingest Pipelineを設定（ドキュメント投入時に自動ベクトル化）
3. Search Pipelineを設定（クエリテキストを自動ベクトル化）

### Knowledge Bases との比較

| | Knowledge Bases | Neural Search |
|---|---|---|
| 運用負荷 | 最小 | やや高い（パイプライン設定必要） |
| カスタマイズ | 限定的 | 柔軟（独自モデル・複雑なパイプライン） |
| 向いている場面 | すぐ使いたい | 既存OpenSearch資産・細かい制御 |

---

## Elasticsearch vs OpenSearch

```
Elasticsearch（OSS）
  ↓ AWSがマネージドサービスとして提供
Amazon Elasticsearch Service
  ↓ ライセンス問題でAWSがフォーク（2021年）
Amazon OpenSearch Service ← 現在の正解
```

| | Elasticsearch | OpenSearch |
|---|---|---|
| ベクトル検索 | ❌ なし | ✅ あり（k-NN・Neural Search） |

---

## Amazon Titan Embeddings V2 の実態（公式確認済み）

### 実際のパラメータ（3つのみ）

| パラメータ | 内容 |
|---|---|
| `dimensions` | 256/512/1024の次元数選択 |
| `normalize` | L2正規化のオン/オフ |
| `embeddingTypes` | float（通常）/ binary（圧縮）の選択 |

### 重要な事実

- 「金融用Titan」「医療用Titan」等のドメイン特化モデルは**存在しない**
- **ファインチューニング非対応**（Custom model support = N/A）
- 「ドメイン固有の最適化」= パラメータ調整の余地がある、という意味にすぎない
- バッチ処理：低レイテンシモード（検索時）/ 高スループットモード（インデックス構築時）

## Amazon Comprehend の位置づけ

**エンティティ抽出・感情分析・キーフレーズ抽出**が主目的。構文レベルの解析。

```
Comprehend が得意：「この文に何が書いてあるか」を分類する
Comprehend が苦手：「クエリを専門用語で膨らませる」（= クエリ拡張）
```

| やること | 正解サービス |
|---|---|
| クエリ拡張（専門用語で膨らませる） | **Bedrock FM** |
| エンティティ抽出・PII検出 | **Comprehend** |
| 感情分析・言語検出 | **Comprehend** |

## Dense / Sparse ベクトルとハイブリッド検索

### 2種類のベクトルの本質

| | 高密度ベクトル（Dense） | スパースベクトル（Sparse） |
|---|---|---|
| 中身 | ほぼ全要素に値がある | ほとんどが0、単語位置だけ値がある |
| 対応する検索 | **ニューラル検索（意味的類似性）** | **BM25（キーワード一致）** |
| 得意なこと | 「デリバティブ」≈「派生商品」と認識 | 「デリバティブ」という単語を含む文書を優先 |

```
スパースベクトルのイメージ：
「デリバティブのリスク」
→ [0, 0, 0, 3.2, 0, 0, 1.8, 0, ...]
              ↑              ↑
         「デリバティブ」  「リスク」
出現した単語の位置だけ値を持つ → ほとんど0 → スパース（疎）
```

### ハイブリッド検索 = Dense + Sparse の組み合わせ

```
クエリ → Dense検索（意味）+ Sparse検索（キーワード）
      → 両スコアをマージ
      → 「柔軟な関連性チューニング」が可能
```

**スコア統合式（Task 4.2）：**
```
final_score = α × vector_score + (1-α) × bm25_score
例: 0.7 × 0.85 + 0.3 × 0.90 = 0.865
```
αはヒューリスティックで調整（ドメイン・用途による）。

**カスタムスコアリング（鮮度・権威スコア加算）：**
- **KB Retrieve API経由では不可**（searchType=HYBRID のみ指定可）
- OpenSearch直接クエリが必要（`function_score`: gauss鮮度減衰 / field_value_factor権威スコア）
- 実装コストとのトレードオフ：細かい調整が必要な場合のみOpenSearch直接を選択

## リランカーが必要な理由

### ベクトル検索の限界

```
クエリ「デリバティブの損失リスクの法的責任」

ベクトル検索の上位結果（意味が近いだけで並ぶ）：
  1位：「デリバティブの概要」← 意味は近いが答えではない
  2位：「リスク管理の基本」← 部分的にしか関連しない
  3位：「損失に対する法的責任」← 本当に欲しいのはこれ
```

**「単語・意味の近さ」でランク付けするとクエリの意図全体が失われる。**

### リランカーの役割

```
ベクトル検索で多めに取得（Top-20等）
  ↓
Bedrock Rerank API がクエリ全体のコンテキストで再スコアリング
  ↓
本当に関連性の高いものが上位に浮上（Top-5等に絞る）
```

---

## MCP（Model Context Protocol）

### 本質

**「FMとツール・データを繋ぐUSB規格」**

```
MCPがない世界：
  FM × ツールの組み合わせごとに個別実装
  → Claude用・Nova用・Llama用それぞれ別のコード

MCPがある世界：
  MCP対応なら何のFMでも同じI/Fで接続
  → 標準化された1つの実装で複数FMに対応
```

### 構造

```
MCPサーバー（コードで実装・ツール提供側）
  ├── Knowledge Base
  ├── データベース
  └── 外部API

MCPクライアント（FMフレームワーク側・組み込み済み）
  ├── Claude / Nova / Llama
  → 開発者がクライアント側コードを書く必要なし
```

### ナレッジベースAPI との関係

```
KB API（中身）= Retrieve / RetrieveAndGenerate API
MCP（アクセス層）= そのAPIを複数FMが同じI/Fで呼べるように包む

KB API ──MCP で包む──→ 複数FMが標準I/Fでアクセス可能
```

## クロスリージョン推論 + SCP

### モデルアクセス有効化はどこで必要か

```
ソースリージョン（呼び出し元）：モデルアクセス有効化が必要 ✅
デスティネーションリージョン（Bedrockが内部でルーティング）：不要 ❌
```

Bedrockが内部でルーティングするため、ユーザーがデスティネーション側でモデルを有効化する必要はない。

### クロスリージョン推論が失敗する原因の切り分け

| 症状 | 原因 |
|---|---|
| 特定リージョンで特定モデルだけ使えない | **モデルアクセス未有効化（ソースリージョン）** |
| **クロスリージョン推論を有効にしたら全部失敗** | **SCPがデスティネーションリージョンへのBedrockアクセスをブロック** |
| IAMロール変更後に失敗 | IAM権限不足 |

### なぜ全リクエストが失敗するか

```
SCPがus-east-1/us-west-2のみBedrockを許可
  ↓ クロスリージョン推論を有効化
BedrockがUS推論プロファイルの他リージョン（us-east-2等）にルーティング
  ↓
SCPが「そのリージョンへのBedrock APIは拒否」
  ↓
全リクエスト失敗
```

---

## OpenSearch Ingestion

**「OpenSearch専用のサーバーレスデータ取り込みパイプライン」**

```
データソース（S3・Kafka・DynamoDB・HTTP等）
  ↓ YAMLでパイプライン定義
OpenSearch Ingestion
  ├── データ変換・フィルタ
  ├── エンリッチメント
  └── Bedrock埋め込みモデルでベクトル化
  ↓
OpenSearch Service / Serverless
```

### Glue との違い

| | OpenSearch Ingestion | Glue / Glue DataBrew |
|---|---|---|
| 目的 | **OpenSearchへの取り込み専用** | 汎用ETL・データ変換 |
| 出力先 | OpenSearchのみ | S3・RDS・Redshift等なんでも |
| 設定 | YAMLパイプライン | GUIまたはコード |
| ベクトル化 | Bedrockと連携して可能 | 別途Lambda等が必要 |

## クエリ処理・検索品質向上パターン

### クエリ拡張（Query Expansion）

ユーザーの入力クエリをFMが自動で膨らませてから検索する手法。

**なぜ必要か：ユーザーの言葉 ↔ 文書の言葉のギャップ**

```
ユーザー入力：「会社が約束を破った場合の補償について」
       ↓ Bedrock FM がクエリ拡張
拡張後：「契約不履行 損害賠償 債務不履行 民法415条
         履行遅滞 履行不能 不完全履行 填補賠償 逸失利益」
       ↓ 拡張クエリでベクトル検索
→ 専門用語で書かれた法規制・判例がヒットする
```

**RAGの中での位置づけ：**

```
通常のRAG：
  クエリ → ベクトル検索 → チャンク取得 → FM応答

クエリ拡張あり：
  クエリ → FM（拡張） → 拡張クエリ → ベクトル検索 → チャンク取得 → FM応答
             ↑ここが追加
```

### クエリ処理パターン比較（試験頻出）

| 手法 | 内容 | 向いている場面 |
|---|---|---|
| **クエリ拡張** | クエリを専門用語で膨らませる | ユーザーの日常語 ↔ 専門用語のギャップ埋め |
| **HyDE**（仮説文書埋め込み） | 「こんな文書が答えになるはず」という仮想文書を生成してそのベクトルで検索 | クエリが短く曖昧な場合 |
| **クエリ分解** | 複雑な質問を複数のサブクエリに分割して並列検索 | 多要素の複雑なクエリ |
| **クエリ書き換え** | 曖昧な表現を明確な表現に言い換える | ユーザーの表現がざっくりしている場合 |

## 大規模ベクトル検索のアーキテクチャ選択（AIP-43）

### スケール別の最適解

| 規模・要件 | 推奨サービス | 理由 |
|---|---|---|
| RAGドキュメント〜数百万件、汎用 | **Bedrock Knowledge Base** | フルマネージドRAG、チャンク管理・引用生成込み |
| 1億件超・500ms SLA・ピーク5倍 | **OpenSearch Service k-NN** | HNSWアルゴリズム＋シャーディングで水平スケール |
| 企業内ドキュメント検索（構造化検索でOK） | Amazon Kendra | ドキュメント意味検索向け、大規模ベクトル類似性は不向き |
| 小〜中規模の既存RDS連携 | Aurora pgvector | 1億件超のスケールアウトには限界あり |

### OpenSearch k-NN の特徴

- **アルゴリズム**: HNSW（Hierarchical Navigable Small World）または IVF
- **水平スケール**: シャーディングによりインデックスを複数ノードに分散
- **達成可能なSLA**: 1億件超でも 500ms 以内の近似最近傍探索が可能
- **フルマネージド**: AWS がノード管理・レプリケーション・スケーリングを担当

#### HNSW vs IVF 比較（AIP-56 補足）

| 観点 | HNSW | IVF（Inverted File Index） |
|---|---|---|
| **検索精度** | 高い | HNSWより低下リスクあり |
| **インデックスサイズ** | 大きい | 小さい（圧縮可能） |
| **検索速度** | 高速 | データ量が多い場合に有利 |
| **用途** | デフォルト推奨・高精度が必要な場合 | 大規模データでストレージを節約したい場合 |
| **注意点** | ストレージコスト増 | 精度が落ちる可能性 |

#### HNSWパラメータ（Task 4.2）

インデックス**作成時**に設定（後から変更は再インデックス必要）。

| パラメータ | 上げると | 下げると |
|---|---|---|
| **m**（接続数） | 精度↑・メモリ↑・構築遅 | 精度↓・メモリ↓・構築速 |
| **ef_construction**（探索幅） | 精度↑・構築遅 | 精度↓・構築速 |

```
一般的な目安: m=16, ef_construction=100
精度優先:     m=32, ef_construction=256
速度優先:     m=8,  ef_construction=50
```

## RAG チャンキング戦略と RetrieveAndGenerate API（AIP-54）

### 問題のコンテキスト
- Bedrock Knowledge Base（マネージドRAG）を使っていても、**チャンキングが悪いと回答品質が落ちる**
- 固定サイズ分割（例: 400トークン）では、論理的に関連するセクションが別チャンクに分断される
- 「コンテキストウィンドウ超過」の問題 = RAGで取得したチャンクに必要な文脈が含まれていない

```
固定400トークン分割の問題例（臨床試験報告書）：
  [チャンク1] 試験デザイン・方法
  [チャンク2] 結果（数値データ）  ← クエリにヒットするが文脈欠落
  [チャンク3] 考察・副作用評価
→ チャンク2だけ取得しても意味が不完全
```

### セマンティックチャンキング（Bedrock Knowledge Base 標準機能）

文ごとに埋め込みベクトルを生成し、**隣接する文間のコサイン距離が大きくなるポイントを境界**にする。

| パラメータ | 意味 |
|---|---|
| **ブレークポイントパーセンタイルしきい値** | 文間距離の上位N%のみを境界とする（95%=上位5%の大きな断絶のみ境界） |
| **バッファサイズ** | 前後N文を含めて埋め込みを生成（例: 3文→文脈が豊かに） |

```
セマンティックチャンキング結果例：
  [チャンク1] 試験デザイン+方法+結果+考察（意味的まとまり）
  [チャンク2] 副作用評価セクション全体
→ 関連情報が同一チャンクに収まる
```

#### セマンティックチャンキングがレイテンシーにも好影響を与える理由（AIP-56）

1. **チャンクが自己完結** → ベクトル空間上でクエリとの類似度計算がより正確になる
2. **ノイズが減る** → 取得チャンク数（top-K）を少なくしても必要な情報が得られる
3. **LLMへの入力トークン量が減る** → 推論コストとレイテンシーが改善

```
固定分割：関連情報が複数チャンクに分散 → top-K を大きくしないと取りこぼし → 入力トークン増 → レイテンシー悪化
セマンティック：関連情報が同チャンク内に収まる → 少ないチャンクで足りる → 入力トークン減 → レイテンシー改善
```

> ポイント：「インフラ増強（シャード追加・インスタンス強化）」ではチャンク品質の問題は解消しない。根本原因（データ品質）から直す。

### 固定サイズ vs セマンティック vs 階層的チャンキング

| 方式 | 特徴 | 向いているケース |
|---|---|---|
| **固定サイズ** | 実装簡単、意味無視 | 文書構造が均一な場合 |
| **セマンティック** | 意味的まとまりで分割、Bedrockネイティブ対応 | 技術文書・論文・マニュアル |
| **階層的（親子）** | 親チャンクで文脈、子チャンクで精密検索 | 長文で大局と細部を両立したい場合 |

### RetrieveAndGenerate API

RAGパイプライン全体を**1回のAPI呼び出しで完結**するマネージドAPI。

```
RetrieveAndGenerate API の処理：
  ① クエリを埋め込みベクトルに変換
  ② ベクトルストアから類似チャンクを取得
  ③ チャンクをプロンプトに組み込んでFMで回答生成
  ④ 引用情報（出典）を自動付与
```

- 自前で Retrieve → Prompt組み立て → InvokeModel を実装する必要がない
- 引用付き回答を標準でサポート（AIP-55でも重要）

### Amazon Bedrock Knowledge Base の構造

```
Bedrock Knowledge Base（マネージドRAGサービス）
  ├─ データソース（S3, Confluence, SharePoint など）
  ├─ チャンキング設定（固定 / セマンティック / 階層的）
  ├─ 埋め込みモデル（Titan Embeddings など）
  └─ ベクトルストア（選択式）
       ├─ Amazon OpenSearch Serverless（デフォルト）
       ├─ Amazon Aurora (pgvector)
       └─ Pinecone / MongoDB Atlas など
```

**Amazon Kendra との違い**：
- Kendra = キーワードベースのエンタープライズSearch（RAGではない）
- Bedrock Knowledge Base = ベクトル検索ベースのマネージドRAG

## OpenSearch ハイブリッド検索（AIP-62）

### ベクトル検索 vs レキシカル検索の弱点
| 検索方式 | 強み | 弱み |
|---|---|---|
| **セマンティック検索（k-NN/ベクトル）** | 意味的な類似性、自然言語表現 | 略称・専門用語・固有名詞の取りこぼし |
| **BM25 レキシカル検索** | 文字列完全一致、略称確実ヒット | 意味的類似性を捉えられない |
| **ハイブリッド（両者を統合）** | 両方の強みを補完 | なし（OpenSearch Search Pipeline で実現） |

### OpenSearch Search Pipeline の仕組み
- **normalization-processor**: ベクトルスコアとBM25スコアをそれぞれ正規化（min-max正規化/L2正規化）
- **combination-processor**: 重み付き加算で統合スコア算出（例：セマンティック0.7、レキシカル0.3）
→ 用語を含まない文献のランクが相対的に下がり、実際に用語を含む関連文献が上位に

## MemoryDB Flat インデックス vs HNSW（AIP-66）

### ① Flat インデックスとは何か（概念）

```
Flat = ブルートフォース（総当たり）= Exact NN（完全最近傍探索）

動き：
  クエリベクトル → 全データと距離を1件ずつ計算 → 近い順に返す

  データ1,000件   → 1,000回計算
  データ1,000,000件 → 1,000,000回計算  ← O(n) の計算量

なぜ 100% recall が保証されるか？
  → 全件計算しているので「見落とし」が物理的に発生しない
  → 正解がどこにあっても必ず見つかる

⚠ GuardDutyの「ブルートフォース検知」との混同に注意
  GuardDuty: 攻撃者が力任せに試みる行為を"検知"する
  Flat: 自分が力任せに計算を"実行"する → 同じ単語だが全く別の文脈
```

### ② HNSW との比較・トレードオフ

```
HNSW（Hierarchical Navigable Small World）= 階層グラフ構造

動き：
  事前にデータを階層グラフで整理 → クエリ時は近そうな方向だけ辿る
  → O(log n) 程度 → 数百万件でも高速

代償：
  グラフを"近道"で辿るため、本当の最近傍を見逃すことがある

データ規模 vs 精度のトレードオフ：
┌──────────────────┬──────────┬──────────┐
│                  │  Flat    │  HNSW    │
├──────────────────┼──────────┼──────────┤
│ 数千〜数万件     │ ✅ 速い  │ ✅ 速い  │
│ 数十万〜数百万件 │ ❌ 遅い  │ ✅ 速い  │
│ Recall 100%      │ ✅ 保証  │ ❌ 非保証│
│ 医療・法律用途   │ ✅ 最適  │ △ リスク │
└──────────────────┴──────────┴──────────┘

→ 「数千件 + 精度最優先」= Flat
→ 「大規模 + recall 95% 許容」= HNSW
```

### ③ ef_search について（HNSW のパラメータ）

```
ef_search = HNSWの探索範囲パラメータ

ef_search を上げる → より広い範囲を探す → recallが上がりやすい・遅くなる
ef_search を下げる → 範囲を絞る → 速いがrecallが下がる

重要：ef_search をどれだけ上げても 100% recall は保証されない
  理由：グラフ構造自体にバイアスがかかっており、
        グラフが繋がっていない部分は探索対象外になる

→ 100% recall が必要 → ef_search を調整しても無意味 → Flat 一択
→ ef_search = recall確率を高める手段であり、保証の手段ではない
```

### ④ MemoryDB のシャード（分散の仕組み）

```
MemoryDB（Redis互換）のシャード = データの水平分割単位

例：ベクトルデータ 100万件
  シャード1: 0〜33万件（プライマリ + レプリカ）
  シャード2: 33〜66万件（プライマリ + レプリカ）
  シャード3: 66〜100万件（プライマリ + レプリカ）

役割：
  ・スケールアウト（1ノードで持てないデータ量を分散）
  ・並列検索（各シャードが同時に検索 → 全体レイテンシー低減）
  ・可用性（各シャードにレプリカ → ノード障害時も継続）

Flat 検索との組み合わせ：
  各シャードで総当たり計算 → 全シャード結果をマージ
  シャード数を増やす → 並列度UP → スループット向上
```

### ⑤ Redis / MemoryDB の永続性（よくある誤解）

```
よくある誤解：「Redisはインメモリ = 揮発性 = 永続化できない」

Redis（OSS）：デフォルトはインメモリ。AOF/RDB設定で永続化はできるが"おまけ"感

Amazon MemoryDB for Redis：別物！
  ┌──────────────────────────────────────┐
  │ Multi-AZ Transactional Log で       │
  │ 全書き込みをAZをまたいだ耐久性ある  │
  │ ストレージに同期書き込み            │
  └──────────────────────────────────────┘
  → データ消えない。RDS と同等の耐久性
  → インメモリ（速度）+ 永続化（耐久性）を両立

→ ガイドライン文書のベクトル化データを保存しても問題なし
→ 「Redisだから揮発性」という先入観は捨てる
```

### ⑥ Amazon Neptune とは

```
Amazon Neptune = グラフデータベース（Graph DB）

グラフDB = ノード（点）とエッジ（繋がり）でデータを表現
  例：「AさんはBさんの友人で、BさんはCさんの同僚で...」の関係性を辿る
     「この化合物はこの酵素に結合して...」の連鎖構造

ユースケース：
  ・SNSの友人推薦
  ・不正検知（取引ネットワーク）
  ・知識グラフ

ベクトル検索については：
  Neptune Database（本体）→ ベクトル類似性検索 非対応
  Neptune Analytics（別サービス）→ ベクトル検索対応

試験では：
  「Neptuneはグラフ用。ベクトル検索にはNeptune Databaseは使えない」
  「Neptune Analyticsは別物として区別する」
```

### ⑦ Aurora pgvector の正しい使いどころ

```
誤解：「Aurora pgvectorはk-NNが使えない」
→ 実際はHNSWとIVFFlatの両方使える

Aurora pgvector の特性：
  ・ PostgreSQL の拡張機能（pg_vector）
  ・ディスクベース（通常のRDBと同じ）
  ・既存のAurora PostgreSQLのテーブルと同じDBに置ける
  ・ACIDトランザクション保証あり

✅ 使うべき場面：
  「すでにAurora PostgreSQLでデータ管理している」
  「ベクトル検索の精度・速度はある程度で妥協できる」
  「別サービスを増やしたくない（コスト・運用シンプル化）」
  「ベクトル検索と通常のSQLを同一トランザクションで扱いたい」
  「レイテンシーは数十〜数百ミリ秒程度許容できる」

❌ 向かない場面：
  「100% recall 必須」→ HNSWは近似検索
  「ミリ秒以下の超低レイテンシーが必要」→ ディスクベースがボトルネック
  「数十億件規模のベクトル」→ ディスクI/Oが限界

→ 無条件に外すのではなく「既存Aurora活用 + レイテンシー許容」なら候補に入る
```

### ⑧ OpenSearch がディスクベースでも速い理由

```
OpenSearch（ディスクベース）でも速い理由：

① HNSWインデックスをメモリにキャッシュ
  → よく使われるインデックスはRAMに載っている（ホットデータ）

② 近似検索（ANN）
  → 全件スキャンしない → ディスクアクセスを最小化

③ 分散アーキテクチャ
  → シャード並列検索でスループット向上

ただし MemoryDB には勝てない：
  MemoryDB: 全データが最初からメモリ上 → ディスクアクセスゼロ
  OpenSearch: キャッシュに乗ってないデータはディスクI/Oが発生

速さの序列（おおよそ）：
  MemoryDB（Flat/HNSW） > OpenSearch HNSW > Aurora pgvector HNSW
```

### ベクトル検索インデックスの種類と特性

| 項目 | **Flat（総当たり）** | **HNSW（階層グラフ）** |
|---|---|---|
| **計算量** | O(n) | O(log n) 程度 |
| **検索方式** | Exact NN（完全最近傍） | ANN（近似最近傍） |
| **Recall** | **100%保証** | 100%未満（近似） |
| **速度** | データ量に比例して線形増加 | 大規模でも高速 |
| **適したデータ規模** | 小〜中規模（数千〜数万件） | 大規模（数十万件〜） |
| **インメモリ（MemoryDB）** | マイクロ秒〜ミリ秒 | マイクロ秒〜ミリ秒 |

### 各サービスの特性まとめ

| サービス | インデックス | 検索方式 | Recall | ストレージ | レイテンシー |
|---|---|---|---|---|---|
| **MemoryDB Flat** | Flat | Exact NN | **100%** | インメモリ+永続化 | 最低 |
| **MemoryDB HNSW** | HNSW | ANN | < 100% | インメモリ+永続化 | 最低 |
| **OpenSearch HNSW** | HNSW | ANN | < 100% | ディスク（インデックスはキャッシュ） | 中 |
| **Aurora pgvector HNSW** | HNSW | ANN | < 100% | ディスク（RDS） | 高 |
| **Neptune Database** | なし | **非対応** | N/A | ディスク | N/A |
| **Neptune Analytics** | あり | ANN | < 100% | ディスク | 中 |

## ソースリネージュ（Source Lineage）とその実装（公式模擬 Q12）

### 用語定義

**ソースリネージュ（Source Lineage）**  
「このコンテンツがどのデータソースから生成されたか」を追跡可能にする仕組み。  
日本語では「出典の来歴」「データ系譜」とも言う。  
RAG や コンテンツ生成システムで「信頼性検証」「根拠の透明性」のために必要。

### ソースリネージュの実装パターン（最小オーバーヘッド）

| 手段 | 役割 | オーバーヘッド |
|---|---|---|
| **FM出力へのメタデータタグ付け** | 生成コンテンツに出典情報を直接付与 | 低（処理中に自動挿入） |
| **AWS Glue Data Catalog への登録** | データソース自体を検索可能なインベントリで管理 | 低（マネージドサービス） |

---

## ベクトルストアのバックアップ・リカバリ（Task 4.3）

ストレージサービスごとに仕組みが異なる。

| ストレージ | バックアップ方法 | リカバリ方法 |
|---|---|---|
| **OpenSearch Service** | 自動スナップショット（毎時・S3保存・無料）/ 手動スナップショット | スナップショットから新クラスターに復元 |
| **Aurora PostgreSQL（pgvector）** | 自動バックアップ（1〜35日保持）| **PITR**（ポイントインタイムリカバリ）= 1秒単位で指定時刻に復元 |

**自動化パターン：**

```
EventBridge Scheduler（毎日深夜）
    ↓
Lambda
    ├─ OpenSearch スナップショット API 呼び出し
    └─ Aurora バックアップ有効性の確認
    ↓
結果を CloudWatch メトリクスに記録 → 失敗時にアラーム
```

**試験の判断軸：**
```
「秒単位の復元・データ欠損最小化」         → Aurora PITR
「インデックス全体の復元」                 → OpenSearch スナップショット
「バックアップの自動化・スケジュール実行」  → EventBridge Scheduler + Lambda
```

